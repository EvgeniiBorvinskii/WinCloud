# 🚀 БЫСТРЫЙ СТАРТ WINCLOUD

## ✅ ЧТО УЖЕ СДЕЛАНО:

Создан полнофункциональный архиватор WinCloud с:
- 💻 Клиентским приложением (GUI как у WinRAR)
- ☁️ Серверной частью (REST API)
- 🔐 Шифрованием (AES-256)
- 📊 Разделением файлов 10%/90%
- 📝 Полной документацией

## 📋 ЧТО НУЖНО СДЕЛАТЬ:

### 1. ДОБАВИТЬ ЛОГОТИПЫ (важно!)

Создайте или добавьте изображения в папку `assets/`:
- `wincloud.png` - основной логотип (512x512px)
- `wincloud2.png` - альтернативный логотип
- `wincloud.ico` - иконка для Windows

**Временно можно создать простые placeholder изображения**

### 2. ПРОТЕСТИРОВАТЬ ЛОКАЛЬНО

```powershell
# Перейдите в папку проекта
cd C:\Users\KeyWest\Desktop\PROJECTS\WinCloud

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python wincloud_client/main.py
```

**Ожидаемый результат:**
- Откроется окно WinCloud
- Будет предупреждение о недоступности сервера (это нормально до развертывания)
- Можно тестировать интерфейс

### 3. СОБРАТЬ EXECUTABLE (опционально)

```powershell
# Запустите скрипт сборки
.\build.ps1
```

**Результат:** `dist/WinCloud.exe`

### 4. РАЗВЕРНУТЬ НА СЕРВЕРЕ 5.249.160.54

#### Вариант А: Автоматическое развертывание

```bash
# 1. Скопируйте серверные файлы
scp -r wincloud_server root@5.249.160.54:/tmp/

# 2. Подключитесь к серверу
ssh root@5.249.160.54

# 3. Выполните развертывание
cd /tmp/wincloud_server
chmod +x deploy.sh
./deploy.sh
```

#### Вариант Б: Ручное развертывание

```bash
ssh root@5.249.160.54

# Создайте пользователя
useradd -r -s /bin/bash -d /srv/WinCloud wincloud

# Создайте директории
mkdir -p /srv/WinCloud/{storage,logs,backups}

# Скопируйте файлы server.py и requirements.txt в /srv/WinCloud

# Установите зависимости
cd /srv/WinCloud
pip3 install -r requirements.txt

# Установите права
chown -R wincloud:wincloud /srv/WinCloud
chmod 700 /srv/WinCloud/storage

# Создайте systemd сервис (см. deploy.sh)
# Запустите сервис
systemctl start wincloud
systemctl enable wincloud
```

#### Проверка развертывания:

```bash
# Проверьте статус сервиса
systemctl status wincloud

# Проверьте доступность
curl http://localhost:8443/api/v1/health

# Посмотрите логи
journalctl -u wincloud -f
```

### 5. НАСТРОИТЬ FIREWALL

```bash
# Откройте порт 8443
ufw allow 8443/tcp
# или
firewall-cmd --permanent --add-port=8443/tcp
firewall-cmd --reload
```

### 6. ОПУБЛИКОВАТЬ НА GITHUB

```bash
cd C:\Users\KeyWest\Desktop\PROJECTS\WinCloud

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: WinCloud v1.0.0 - Cloud-based archiver"

# Добавьте remote репозиторий
git remote add origin https://github.com/EvgeniiBorvinskii/WinCloud.git

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

**ВАЖНО:** `.gitignore` уже настроен, серверный код НЕ будет загружен на GitHub!

### 7. ПРОТЕСТИРОВАТЬ ПОЛНУЮ РАБОТУ

После развертывания сервера:

1. Запустите клиент: `python wincloud_client/main.py`
2. Добавьте файлы
3. Создайте архив
4. Проверьте, что:
   - Файлы сжимаются
   - Прогресс отображается
   - Данные загружаются на сервер
   - Создается .wca файл
5. Попробуйте извлечь архив
6. Проверьте целостность файлов

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ:

### Клиент не запускается:
```powershell
pip install --upgrade PyQt6 zstandard cryptography requests
```

### Сервер не стартует:
```bash
journalctl -u wincloud -n 50
python3 /srv/WinCloud/server.py  # Запустите вручную для отладки
```

### Ошибка подключения:
- Проверьте firewall: `ufw status` или `firewall-cmd --list-all`
- Проверьте порт: `netstat -tlnp | grep 8443`
- Проверьте IP в конфиге клиента: `~/.wincloud/config.json`

## 📊 СТРУКТУРА ПРОЕКТА:

```
WinCloud/
├── wincloud_client/    ← Клиент (GUI + логика) [PUBLIC]
├── wincloud_server/    ← Сервер (НЕ публикуется) [PRIVATE]
├── common/             ← Общие модули [PUBLIC]
├── assets/             ← Логотипы и ресурсы [PUBLIC]
├── docs/               ← Документация [PUBLIC]
├── tests/              ← Тесты [PUBLIC]
└── README.md           ← Главная документация [PUBLIC]
```

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

1. ✅ Добавить логотипы в `assets/`
2. ✅ Протестировать локально
3. ✅ Развернуть сервер
4. ✅ Протестировать полный цикл
5. ✅ Опубликовать на GitHub
6. 📈 Собрать feedback
7. 🚀 Развивать дальше (см. TODO.md)

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ:

### Локальная разработка:
```powershell
# Запуск
python wincloud_client/main.py

# Тесты
pytest tests/ -v

# Сборка
.\build.ps1
```

### Сервер:
```bash
# Логи
journalctl -u wincloud -f

# Рестарт
systemctl restart wincloud

# Статус
systemctl status wincloud
```

### Git:
```bash
# Статус
git status

# Добавить изменения
git add .

# Коммит
git commit -m "Update: ..."

# Отправить
git push
```

## 📞 ПОДДЕРЖКА:

- **GitHub Issues**: https://github.com/EvgeniiBorvinskii/WinCloud/issues
- **Документация**: См. папку `docs/`
- **Логи клиента**: `C:\Users\KeyWest\.wincloud\logs\`
- **Логи сервера**: `journalctl -u wincloud`

---

## ✨ ГОТОВО К ИСПОЛЬЗОВАНИЮ!

Проект полностью готов к:
- ✅ Локальному тестированию
- ✅ Развертыванию на production сервере
- ✅ Публикации на GitHub
- ✅ Использованию реальными пользователями

**Удачи с запуском WinCloud! 🚀**
